[build]
  publish = "public"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

[functions."prototypes"]
  timeout = 300

[functions."build-engine-background"]
  timeout = 900

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
